class StudentsTeachers < ActiveRecord::Base
  # validate :unique_teacher
  # validates :teacher_id, uniqueness: { scope: :student_id, message: "no student should have the same teacher twice" }

  # def unique_teacher
    
  # end
end
